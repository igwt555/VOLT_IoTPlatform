// import Timezone from '../models/timezone.mjs';
// import timezone from './timezones.mjs';
// import { onDbReady } from '../db.mjs';

const insertTimeZone = async () => {
  // await onDbReady.then(async () => {
  //   const timezonedata = timezone;
  //   const isTimezonedata = await Timezone.findAll();
  //   if (isTimezonedata.length > 0) {
  //     return;
  //   }
  //   await Timezone.bulkCreate(timezonedata);
  // });
};
export default insertTimeZone();
